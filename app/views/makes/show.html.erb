<p id="notice"><%= notice %></p>

<table>
  <thead>
  <tr>
    <th>Name:</th>
    <td><%= @make.name %></td>
    <th>Models</th>
  </tr>
  </thead>

  <tbody>
  <tr>
    <td></td>
    <td></td>
    <td>
      <ol>
        <% @make.cars.each do |car| %>
          <li> <%= car.name %>
        <% end %>
      </ol>
    </td>
  </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_make_path(@make) %> |
<%= link_to 'Back', makes_path %>
