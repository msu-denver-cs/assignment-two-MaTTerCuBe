<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @country.name %>
</p>

<p>
  <strong>Makes:</strong>

  <ol>
    <% @country.makes.each do |make| %>
    <li> <%= make.name %>
  <% end %>
  </ol>
</p>

<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>
